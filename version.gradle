ext.VERSION_CODE = 1
ext.VERSION_NAME = '1.1.0'

if ("on".equalsIgnoreCase(BUILD_SNAPSHOT)) {
    ext.VERSION_NAME += "-SNAPSHOT"
}

println("BUILD_SNAPSHOT=" + BUILD_SNAPSHOT)
println("VERSION_NAME=" + ext.VERSION_NAME + ", VERSION_CODE=" + ext.VERSION_CODE)